{
  "name": "this-day-in-history",
  "version": "1.0.0",
  "description": "A bad date picker. Or a good one.",
  "main": "build.js",
  "scripts": {
    "start": "yarn build && http-server ./",
    "build": "browserify index.js -o build.js"
  },
  "repository": "git@github.com:weareredshift/this-day-in-history.git",
  "author": "Trevor Alexander <alexander.trevor@mail.com>",
  "license": "UNLICENSED",
  "dependencies": {
    "chrono-node": "^1.3.4",
    "http-server": "^0.10.0",
    "wikijs": "^3.1.0"
  },
  "devDependencies": {
    "browserify": "^14.4.0"
  }
}
